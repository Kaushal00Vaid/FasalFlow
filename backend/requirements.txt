# Runtime dependencies only. Training deps are in requirements-dev.txt.
#
# xgboost-cpu avoids pulling nvidia-nccl-cu12 (~300 MB) which the regular
# `xgboost` wheel depends on. This is the single biggest memory saver
# for Render's 512 MB free tier.
pandas>=2.0,<3
numpy>=1.24,<3
pyarrow>=14.0,<25          # needed at runtime for pd.read_parquet
xgboost-cpu>=2.0,<4         # CPU-only build; unpickles xgboost-trained model
fastapi>=0.110
uvicorn>=0.27               # no [standard] — drops uvloop/httptools/watchfiles/websockets
pydantic>=2.0
httpx>=0.27                 # used by /pitch endpoint to call Gemini API
