# Dev / training / test dependencies. NOT installed on Render.
# Install locally with: pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

scikit-learn>=1.3          # used by training pipeline + anomaly model
xgboost>=2.0               # full xgboost (with CUDA) for local training if desired
pytest>=8.0
httpx>=0.27                # for test client / local API smoke tests
